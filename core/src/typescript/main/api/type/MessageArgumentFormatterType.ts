/**
 * Formatter function which allows formatting of message arguments, used by the MessageFormatter to format
 * arguments for the message.
 */
export type MessageArgumentFormatterType = (arg: any) => string;
