{
  "extends": "../tsconfig.base.json",
  "compileOnSave": true,
  "compilerOptions": {
    "rootDir": ".",
    "lib": [
      "ES2015",
      "DOM"
    ],
    "types": []
    // We don't want jest types, as that enables esXXXX version, way newer than what we want (see lib), instead compile must fail.
  },
  "include": [
    "./src/typescript/main/**/*"
  ]
}
